import React from 'react';
import Intro from '../Hidration/Componoment/Intro';

const HomePage = ({ navigation }) => {
    return (

        <Intro navigation={navigation} />


    );
};

export default HomePage;